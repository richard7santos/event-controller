import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    empty: {
        textAlign: 'center',
        marginTop: 20,
        fontSize: 16,
        color: '#888',
    },
    container: {
        flex: 1,
        padding: 10,
        backgroundColor: '#f9f9f9',
    },
});
